describe('Example app', () => {
  beforeEach(async () => {
    await device.reloadReactNative();
  });

  // it('should have menu screen', async () => {
  //   await expect(element(by.id('menu'))).toBeVisible();
  // });

  // it('should display calendars screen', async () => {
  //   await element(by.id('calendars_btn')).tap();
  //   await expect(element(by.id('calendars_screen'))).toBeVisible();
  // });
});
